{
  "name": "jay-trainer-music",
  "version": "1.0.0",
  "description": "Jay Trainer — Music from Pittsburgh, Pennsylvania",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy _site --project-name jay-trainer-music"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "dependencies": {
    "stripe": "^22.1.1"
  }
}
